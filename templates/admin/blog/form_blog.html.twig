{% extends 'admin/index_admin.html.twig' %}

{% block title %}{{ title }}{% endblock %}


    {% block content %}
        <div class="container">
            <div class="row justify-content-center align-items-center">
                <h2 class="text-center mt-xl-3">Добавить Блог</h2>
                <div class="col-md">
                    <div class="card">
                        <div class="card-body">
                            {{ form_start(blog_form) }}
                            {{ form_row(blog_form.image) }}
                            {{ form_row(blog_form.title) }}
                            {{ form_row(blog_form.content) }}
                            {{ form_row(blog_form.save) }}
                            {{ form_end(blog_form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>

    {% endblock %}

