{% extends 'admin/index_admin.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-body">
            <h5>Добавить задание</h5>
            <hr/>
            <div class="row gy-3">
                {{ form_start(task_form) }}
                {{ form_widget(task_form) }}
                {{ form_end(task_form) }}
            </div>
        </div>
    </div>
{% endblock %}